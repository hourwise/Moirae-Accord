# ACCORD-02E2 — Independent Review Brief

Status: **CANDIDATE — NOT ACCEPTED**

Review the additive registry candidate without modifying accepted ACCORD-02 artifacts.

## Questions

1. Does the accepted C contract require an explicit stage for every settlement target?
2. If yes, can the 23 accepted vectors be resolved without adding stage values or an accepted non-positive contract?
3. Is any proposed mapping supported by an existing accepted profile row and stage vocabulary rather than by evidence-class inference?
4. Does `complete-negative` need a first-class applicability entry distinct from positive effect predicates?
5. Is `resource-visible` intentionally unsupported, or is an accepted semantic row missing?

## Required reviewer outcome

The reviewer must preserve the distinction between:

- an exact registry algorithm that is deterministic; and
- an input corpus that is not yet sufficient to satisfy that algorithm.

The current candidate's deterministic result is `BROADER_SPECIFICATION_REVIEW_REQUIRED`; it must not be upgraded to a complete amendment without an explicit normative decision for the missing stage/negative semantics.
